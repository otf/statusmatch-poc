{
  "scripts": {
    "start": "npm install && npx elm-live --path-to-elm=./node_modules/.bin/elm --dir=public src/Main.elm -- --output=public/dist/elm.compiled.js --debug",
    "build": "npx elm make --output public/dist/elm.compiled.js src/Main.elm --debug"
  },
  "devDependencies": {
    "elm": "^0.19.1-5",
    "elm-format": "^0.8.5",
    "elm-live": "^4.0.2"
  }
}
